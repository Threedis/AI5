name = "expenseverify"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"

# Created once via: npx wrangler d1 create evs-db
# then paste the returned database_id below (a placeholder is fine for
# `wrangler pages dev --local`, which only ever touches a local sqlite file).
[[d1_databases]]
binding = "DB"
database_name = "evs-db"
database_id = "00000000-0000-0000-0000-000000000000"
